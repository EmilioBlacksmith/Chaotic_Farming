/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

[CustomEditor(typeof(taskSystems))]
public class ObjectBuilderEditor : Editor
{
    public override void OnInspectorGUI()
    {
        base.OnInspectorGUI();

        taskSystems myScript = (taskSystems)target;
        if(GUILayout.Button("Try Random"))
        {
            Debug.Log(myScript.randomNum());
        }
    }
}*/
